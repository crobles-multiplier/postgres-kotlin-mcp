# Test Database Configuration
# This file is used for unit tests

# Test Database Connection Properties
database.staging.jdbc-url=jdbc:postgresql://localhost:5433/testdb
database.staging.username=testuser
database.staging.password=testpass

database.release.jdbc-url=jdbc:postgresql://localhost:5433/testdb
database.release.username=testuser
database.release.password=testpass

# HikariCP Configuration for Tests (Optional)
# Uses sensible defaults if not specified

# Optional: Override default pool sizes for testing
hikari.staging.maximum-pool-size=5
hikari.staging.minimum-idle=1

hikari.release.maximum-pool-size=8
hikari.release.minimum-idle=2
